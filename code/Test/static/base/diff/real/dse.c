int logicalValue(int t, int currentTime, int dl) {
	int old;
  if (!(currentTime - t >= 100)) {
  	return old;
	} else {
		int val = 0;
		for (int i = 0; i < dl; i++) {
			val = val + i;
		}
		old = val;
		return val;
	}
}
